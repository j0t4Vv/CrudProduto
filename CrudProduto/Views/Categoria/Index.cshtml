@model IEnumerable<CrudProduto.Models.Categorium>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table" id="TableCategoria">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Categoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCategoria" class="btn btn-warning">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCategoria" class="btn btn-info">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCategoria" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
